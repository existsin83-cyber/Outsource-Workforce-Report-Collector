# AGENTS.md

이 파일은 Codex 및 기타 코딩 에이전트가 이 저장소에서 작업할 때 따라야 할 지침이다.
저장소 전체에 적용하며, 더 하위 경로에 별도 `AGENTS.md`가 있으면 해당 범위에서는 하위 파일의
구체적인 지침을 함께 따른다.

## 프로젝트 개요

이 프로젝트는 Outlook에서 외주 일일 작업보고 메일을 수집하고 구조화한 뒤, 사용자가 검토·수정한
승인 행만 기존 Excel 통합문서에 추가하는 Windows 데스크톱 도구다.

- Python 3.12
- PySide6 기반 UI
- Outlook 2019 Desktop COM 연동
- Excel COM 연동
- SQLite 처리 이력 저장
- 한국어 메일 본문, Excel 열 이름 및 사용자 메시지
- 코드 식별자와 주석은 파일의 기존 영문·국문 스타일을 따른다.

요구사항과 설계의 기준 문서는 다음과 같다.

- `docs/PRD.md`
- `docs/TRD.md`
- `docs/SYSTEM_ARCHITECTURE.md`
- `docs/ADR.md`
- `docs/rules.md`

특히 `docs/rules.md`는 변경 불가능한 개발·안전 규칙이다. 파싱, Outlook, Excel, 데이터 모델을
수정하기 전에 반드시 읽는다. `CLAUDE.md`는 프로젝트 배경과 기존 작업 지침을 이해하기 위한
참고 문서로 사용하되, 문서의 상태 설명이 현재 코드보다 오래되었을 수 있으므로 실제 파일과
테스트를 우선 확인한다.

## 작업 시작 절차

1. `HANDOFF.md`의 가장 최근 세션 기록을 읽는다.
2. `git status --short --branch`와 현재 커밋을 확인한다.
3. 기존 수정·미추적 파일과 이번 작업 범위를 구분한다.
4. 요청과 관련된 요구사항·설계·규칙 문서를 읽는다.
5. 실제 실행 경로와 기존 테스트를 확인한 뒤 변경한다.

기존 사용자 변경을 임의로 수정, 삭제, 되돌리거나 현재 작업의 결과로 간주하지 않는다.
사용자가 명시적으로 요청하지 않으면 커밋, 푸시, 브랜치 변경 또는 PR 생성을 하지 않는다.

## 실행 명령

PowerShell 기준:

```powershell
# 개발 환경
py -3.12 -m venv .venv
.venv\Scripts\activate
pip install -e ".[dev]"

# 애플리케이션 실행
python -m outsource_mail_collector.app

# 전체 테스트
pytest

# 대표적인 선택 테스트
pytest tests/test_extraction_pipeline.py
pytest tests/test_extraction_pipeline.py::test_format_b_vendor_header_per_unit_records -v

# Outlook COM PoC
python tools/outlook_poc.py
python tools/inbox_poc.py
```

`pyproject.toml`에 별도 lint 또는 format 명령이 구성되어 있지 않으므로 임의의 도구나 명령을
프로젝트 표준인 것처럼 추가하지 않는다.

## 아키텍처 경계

의존성은 안쪽 계층을 향해야 한다.

```text
ui/             PySide6 위젯과 사용자 상호작용
application/    수집, 추출, 검토, 내보내기 오케스트레이션
domain/         순수 도메인 모델과 상태
parsing/        Outlook·Excel 객체에 의존하지 않는 순수 추출 파이프라인
infrastructure/ Outlook COM, Excel COM, SQLite 구현
```

- UI는 application 서비스를 호출하며 COM 객체를 직접 다루지 않는다.
- parser는 Outlook 객체를 직접 다루지 않는다.
- Excel adapter에 메일 추출 로직을 넣지 않는다.
- domain은 PySide6, pywin32, pandas에 의존하지 않는다.
- COM 객체를 다른 스레드로 전달하지 않는다.
- 구체 구현은 가능한 한 생성자 주입과 명시적인 인터페이스로 연결한다.
- 현재 구현 상태는 문서의 오래된 설명으로 판단하지 말고 `src/`, `tests/`, 최근 커밋으로 확인한다.

## 파싱 및 도메인 불변 조건

파싱 흐름:

```text
normalize → split_sections → extract_work_records → validate
```

다음 값은 하나의 필드로 합치지 않는다.

- `actual_headcount`
- `per_person_man_day`
- `daily_man_day`
- `cumulative_man_day`
- `day_man_day`
- `night_man_day`

추가 규칙:

- 불명확한 숫자를 추정하지 않는다.
- 일일·누적 의미가 명시되지 않은 공수는 해당 필드를 `None`으로 두고 원문 근거를 보존한다.
- 외주 언급이 없는 섹션의 빈 결과 `[]`는 정상적인 “외주 없음” 상태다.
- `confidence` 범위는 0.0부터 1.0까지다.
- 신뢰도 임계값은 0.85 이상 정상, 0.60~0.84 검토 권장, 0.60 미만 검토 필요다.
- 검토 상태는 `domain/models.py`의 `ReviewStatus`를 기준으로 하며 별도 상태 문자열 체계를 만들지 않는다.
- 파서 변경 시 익명화된 실제 형식 fixture를 사용하는 회귀 테스트를 먼저 추가하거나 보강한다.

## Outlook 안전 규칙

Outlook 접근은 읽기 전용이다.

- 메일 삭제 또는 이동 금지
- 읽음·안 읽음 상태 변경 금지
- 자동 회신 또는 전달 금지
- 조회 날짜 범위를 명시
- Exchange 발신자는 필요한 경우 SMTP 주소로 변환
- 처리 식별을 위해 EntryID 보존
- 원본 메일 열기는 Outlook Inspector 사용

`samples/*.msg`는 로컬 전용이며 커밋하지 않는다. 실제 메일 본문, 개인정보 또는 회사 기밀을
소스, fixture, 로그, 문서에 추가하지 않는다.

## Excel 안전 규칙

- 사용자가 검토 화면에서 명시적으로 승인한 행만 내보낸다.
- 실제 파일 쓰기 전 타임스탬프가 포함된 백업을 먼저 만든다.
- 개발과 테스트는 실제 파일의 복사본에서 수행한다.
- 고정 셀 주소를 하드코딩하지 않고 헤더 이름으로 열을 매핑한다.
- 기존 수식, 서식, 병합 셀을 가능한 한 보존한다.
- 파일 존재, 잠금, 읽기 전용 상태 및 권한을 확인한다.
- 백업 성공 전에는 쓰기를 시작하지 않는다.
- Excel 저장 성공 후에만 DB 처리 완료 상태를 기록한다.
- 실제 Outlook 또는 Excel 데이터에 영향을 주는 검증은 사용자의 명시적 승인 없이 실행하지 않는다.

## 데이터·보안 규칙

- 외부 AI 또는 외부 서버 전송은 기본적으로 금지한다.
- 메일 본문 전체를 DB나 로그에 저장하지 않는다.
- 분석 근거에 필요한 최소 구간만 제한적으로 보존한다.
- 로그에는 인증정보, 전체 수신자 목록, 불필요한 개인정보를 넣지 않는다.
- 날짜·시간은 가능한 한 timezone-aware 값으로 다룬다.
- 금액 또는 정밀 소수가 필요하면 부동소수점 대신 `Decimal`을 검토한다.
- 실제 개인·회사 데이터가 포함된 테스트 자료를 커밋하지 않는다.

## 테스트 및 검증

- 버그 수정은 재현 테스트를 먼저 추가한 뒤 구현한다.
- parser 변경은 `tests/fixtures.py`의 익명화 fixture와
  `tests/test_extraction_pipeline.py` 회귀 테스트로 검증한다.
- fixture는 `.txt`가 아니라 Python 문자열 상수로 유지한다. 이 개발 환경에서는 DRM·보안
  에이전트가 일반 텍스트 파일 읽기를 가로챌 수 있다.
- Outlook·Excel COM 코드는 mock 가능한 경계를 유지한다.
- 실제 COM 통합 테스트와 일반 단위 테스트를 구분한다.
- 문서만 변경한 경우 애플리케이션 테스트를 실행하지 않을 수 있지만, 미실행 사실과 이유를 보고한다.
- 완료 보고에는 실행한 명령, 결과, 미실행 검증 및 남은 실환경 검증을 구분해 적는다.
- “코드상 확인”을 “실제 Outlook·Excel·GUI 동작 확인”으로 확대 해석하지 않는다.

Windows에서 pytest 임시 디렉터리 권한 문제가 발생하면 저장소 내부의 작업 전용
`--basetemp` 경로를 사용한다. 기존 사용자 데이터가 있는 경로를 임시 디렉터리로 사용하거나
임의로 정리하지 않는다.

## 변경 관리와 완료 보고

- 작업 전 변경 목적과 예상 파일을 확인한다.
- 기능 변경과 대규모 리팩터링을 한 작업에 섞지 않는다.
- DB 스키마 변경에는 마이그레이션과 이전 버전 호환성 검토를 포함한다.
- 설정 추가에는 기본값과 이전 설정 호환성을 포함한다.
- 변경 후 `git diff --check`, 관련 테스트 및 위험에 비례한 검증을 실행한다.
- 완료 전 `HANDOFF.md`에 현재 세션의 결정, 변경, 검증, 실패, 미완료 사항과 다음 작업을 기록한다.

최종 보고에는 다음을 포함한다.

- 변경 파일
- 구현 또는 문서화한 내용
- 실행한 검증과 결과
- 실행하지 못한 검증
- 남은 위험과 권장 다음 단계
- 커밋·푸시 여부

## 서브 에이전트

- 메인 컨텍스트 보호와 오염 방지를 위해 탐색·분석·테스트·리뷰는 서브 에이전트에 적극 위임한다.
- 서브 에이전트는 역할·난이도·위험도에 맞는 가장 비용 효율적인 모델과 추론 수준을 선택한다.
- 독립 작업은 병렬 수행하되, 토큰 효율을 속도보다 우선하며 중복 작업과 불필요한 에이전트 생성을 피한다.
- 병렬 작업은 최소 인원으로 시작하고, 수정 범위가 겹치거나 선후 의존성이 있으면 순차 수행한다.
- 서브 에이전트에는 필요한 최소 컨텍스트만 제공하고 결과는 간결히 요약하게 한다.
- 중요한 결과는 메인 에이전트가 검증하고 최종 통합한다.

